<footer class="relative bg-gradient-to-b from-white to-green-50 pt-12 pb-6 overflow-hidden">
    <!-- Background Pattern -->
    <div class="absolute inset-0 opacity-5">
        <div class="absolute inset-0" style="background-image: url('data:image/svg+xml,%3Csvg width=\"60\" height=\"60\" viewBox=\"0 0 60 60\" xmlns=\"http://www.w3.org/2000/svg\"%3E%3Cg fill=\"none\" fill-rule=\"evenodd\"%3E%3Cg fill=\"%23059669\" fill-opacity=\"0.1\"%3E%3Ccircle cx=\"30\" cy=\"30\" r=\"2\"/%3E%3C/g%3E%3C/g%3E%3C/svg%3E');"></div>
    </div>

    <!-- CTA Section -->
    <div class="container mx-auto px-6 lg:px-12 mb-12 relative z-10">
        <div class="max-w-4xl mx-auto text-center animate-fade-in">
            <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-800 mb-6 leading-tight">
                KAMI HADIR UNTUK 
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-green-600 to-green-500 animate-pulse">
                    KALIAN!
                </span>
            </h2>
            <p class="text-lg text-gray-600 mb-8 max-w-2xl mx-auto leading-relaxed">
                Memberikan solusi tepat untuk memajukan kelompok tani Winongo Asri dengan teknologi yang inovatif dan mudah diakses
            </p>
            
            <!-- Features Grid -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
                <div class="feature-card animate-slide-up" style="--delay: 0.1s">
                    <div class="feature-icon">
                        <i class="fas fa-rocket text-2xl"></i>
                    </div>
                    <h3 class="feature-title">Efisiensi</h3>
                    <p class="feature-description">Efisiensi adalah <span class="font-semibold text-green-600">kunci</span> kesuksesan</p>
                </div>
                
                <div class="feature-card animate-slide-up" style="--delay: 0.2s">
                    <div class="feature-icon">
                        <i class="fas fa-desktop-alt text-2xl"></i>
                    </div>
                    <h3 class="feature-title">Mudah Diakses</h3>
                    <p class="feature-description">Aplikasi yang <span class="font-semibold text-green-600">mudah diakses</span> di mana saja</p>
                </div>
                
                <div class="feature-card animate-slide-up" style="--delay: 0.3s">
                    <div class="feature-icon">
                        <i class="fas fa-bolt text-2xl"></i>
                    </div>
                    <h3 class="feature-title">Cepat</h3>
                    <p class="feature-description">Penanganan yang <span class="font-semibold text-green-600">cepat</span> dan responsif</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Footer -->
    <div class="bg-gradient-to-r from-green-600 via-green-500 to-green-600 py-8 rounded-t-3xl relative overflow-hidden">
        <!-- Decorative Elements -->
        <div class="absolute top-0 left-0 w-full h-full overflow-hidden">
            <div class="absolute -top-10 -left-10 w-20 h-20 bg-white opacity-10 rounded-full"></div>
            <div class="absolute top-20 right-20 w-16 h-16 bg-white opacity-10 rounded-full"></div>
            <div class="absolute bottom-10 left-1/4 w-12 h-12 bg-white opacity-10 rounded-full"></div>
            <div class="absolute -bottom-5 right-1/3 w-24 h-24 bg-white opacity-10 rounded-full"></div>
        </div>

        <div class="container mx-auto px-6 lg:px-12 relative z-10">
            <div class="flex flex-col lg:flex-row items-center justify-between">
                <!-- Logo Section -->
                <div class="flex items-center space-x-4 mb-6 lg:mb-0 animate-slide-left">
                    <div class="logo-container">
                        <img src="{{ asset('images/logo/logobotani.png') }}" 
                             alt="BO TANI Logo" 
                             class="w-16 h-16 object-contain filter drop-shadow-lg">
                    </div>
                    <div class="text-center lg:text-left">
                        <h3 class="text-2xl font-bold text-white mb-1">BO TANI</h3>
                        <p class="text-green-100 text-sm">Kelompok Tani Winongo Asri</p>
                    </div>
                </div>

                <!-- Copyright Section -->
                <div class="text-center mb-6 lg:mb-0 animate-fade-in" style="--delay: 0.3s">
                    <p class="text-white text-sm">
                        ©{{ date('Y') }} | 
                        <span class="font-bold text-white">
                            BO <span class="text-green-200">TANI</span>
                        </span> | 
                        All Rights Reserved
                    </p>
                    <p class="text-green-100 text-xs mt-1">
                        Made with ❤️ for Indonesian Farmers
                    </p>
                </div>

                <!-- Social Media Section -->
                <div class="flex space-x-6 animate-slide-right">
                    <a href="#" class="social-link" title="Facebook">
                        <i class="fab fa-facebook-f text-xl"></i>
                    </a>
                    <a href="https://www.instagram.com/winongoasri.yk?utm_source=ig_web_button_share_sheet&igsh=ZDNlZDc0MzIxNw==" class="social-link" title="Instagram">
                        <i class="fab fa-instagram text-xl"></i>
                    </a>
                    <a href="#" class="social-link" title="YouTube">
                        <i class="fab fa-youtube text-xl"></i>
                    </a>
                    <a href="https://kirimwa.id/botani" class="social-link" title="WhatsApp">
                        <i class="fab fa-whatsapp text-xl"></i>
                    </a>
                </div>
            </div>

            <!-- Bottom Border -->
            <div class="mt-8 pt-6 border-t border-green-400 border-opacity-30">
                <div class="flex flex-col md:flex-row justify-between items-center text-center md:text-left">
                    <div class="text-green-100 text-sm mb-4 md:mb-0">
                        <span class="flex items-center justify-center md:justify-start">
                            <i class="fas fa-map-marker-alt mr-2"></i>
                            Patangpuluhan, Wirobrajan, Yogyakarta
                        </span>
                    </div>
                    <div class="text-green-100 text-sm">
                        <span class="flex items-center justify-center md:justify-end">
                            <i class="fas fa-phone mr-2"></i>
                            +62 812-3456-7890
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <style>
        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideLeft {
            from {
                opacity: 0;
                transform: translateX(-30px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes slideRight {
            from {
                opacity: 0;
                transform: translateX(30px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes pulse {
            0%, 100% {
                opacity: 1;
            }
            50% {
                opacity: 0.7;
            }
        }

        @keyframes float {
            0%, 100% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-10px);
            }
        }

        .animate-fade-in {
            opacity: 0;
            animation: fadeIn 1s ease-out forwards;
            animation-delay: var(--delay, 0s);
        }

        .animate-slide-up {
            opacity: 0;
            animation: slideUp 0.8s ease-out forwards;
            animation-delay: var(--delay, 0s);
        }

        .animate-slide-left {
            opacity: 0;
            animation: slideLeft 0.8s ease-out forwards;
        }

        .animate-slide-right {
            opacity: 0;
            animation: slideRight 0.8s ease-out forwards;
        }

        .animate-pulse {
            animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
        }

        /* Feature Cards */
        .feature-card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border-radius: 1rem;
            padding: 2rem 1.5rem;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(5, 150, 105, 0.1), transparent);
            transition: left 0.5s ease;
        }

        .feature-card:hover::before {
            left: 100%;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
            border-color: rgba(5, 150, 105, 0.3);
        }

        .feature-icon {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #059669, #10b981);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            margin: 0 auto 1rem;
            transition: all 0.3s ease;
            box-shadow: 0 10px 20px rgba(5, 150, 105, 0.3);
        }

        .feature-card:hover .feature-icon {
            transform: scale(1.1) rotate(5deg);
            box-shadow: 0 15px 30px rgba(5, 150, 105, 0.4);
        }

        .feature-title {
            font-size: 1.25rem;
            font-weight: 700;
            color: #1f2937;
            margin-bottom: 0.5rem;
        }

        .feature-description {
            color: #6b7280;
            line-height: 1.6;
        }

        /* Logo Container */
        .logo-container {
            position: relative;
            transition: all 0.3s ease;
        }

        .logo-container:hover {
            transform: scale(1.1);
        }

        .logo-container::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.2) 0%, transparent 70%);
            transform: translate(-50%, -50%);
            border-radius: 50%;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .logo-container:hover::after {
            opacity: 1;
        }

        /* Social Links */
        .social-link {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 45px;
            height: 45px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            color: white;
            text-decoration: none;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .social-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }

        .social-link:hover::before {
            left: 100%;
        }

        .social-link:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-3px) scale(1.1);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .social-link:active {
            transform: translateY(-1px) scale(1.05);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .feature-card {
                padding: 1.5rem 1rem;
            }

            .feature-icon {
                width: 50px;
                height: 50px;
            }

            .feature-title {
                font-size: 1.125rem;
            }

            .logo-container img {
                width: 3rem;
                height: 3rem;
            }

            .container {
                padding-left: 1rem;
                padding-right: 1rem;
            }

            .text-3xl {
                font-size: 1.875rem;
            }

            .text-4xl {
                font-size: 2.25rem;
            }

            .text-5xl {
                font-size: 3rem;
            }

            .grid-cols-1.md\\:grid-cols-3 {
                grid-template-columns: repeat(1, minmax(0, 1fr));
                gap: 1rem;
            }

            .flex.flex-col.lg\\:flex-row {
                flex-direction: column;
                gap: 1.5rem;
            }

            .text-center.lg\\:text-left {
                text-align: center;
            }

            .flex.space-x-6 {
                justify-content: center;
            }

            .flex.flex-col.md\\:flex-row {
                flex-direction: column;
                gap: 1rem;
            }

            .text-center.md\\:text-left {
                text-align: center;
            }

            .text-center.md\\:text-right {
                text-align: center;
            }

            .justify-center.md\\:justify-start {
                justify-content: center;
            }

            .justify-center.md\\:justify-end {
                justify-content: center;
            }
        }

        @media (max-width: 480px) {
            .feature-card {
                padding: 1rem 0.75rem;
            }

            .feature-icon {
                width: 40px;
                height: 40px;
            }

            .feature-title {
                font-size: 1rem;
            }

            .logo-container img {
                width: 2.5rem;
                height: 2.5rem;
            }

            .text-3xl {
                font-size: 1.5rem;
            }

            .text-4xl {
                font-size: 1.875rem;
            }

            .text-5xl {
                font-size: 2.25rem;
            }

            .social-link {
                width: 40px;
                height: 40px;
            }

            .social-link i {
                font-size: 1rem;
            }
        }

        /* Focus styles for accessibility */
        .social-link:focus {
            outline: 2px solid white;
            outline-offset: 2px;
        }

        .feature-card:focus {
            outline: 2px solid #059669;
            outline-offset: 2px;
        }

        /* Smooth scrolling */
        html {
            scroll-behavior: smooth;
        }
    </style>

    <script>
        // Intersection Observer for animations
        document.addEventListener('DOMContentLoaded', function() {
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };

            const observer = new IntersectionObserver(function(entries) {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            }, observerOptions);

            // Observe all animated elements
            document.querySelectorAll('.animate-fade-in, .animate-slide-up, .animate-slide-left, .animate-slide-right').forEach(el => {
                observer.observe(el);
            });

            // Add hover effects to feature cards
            document.querySelectorAll('.feature-card').forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-10px)';
                });
                
                card.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0)';
                });
            });

            // Add click effects to social links
            document.querySelectorAll('.social-link').forEach(link => {
                link.addEventListener('click', function(e) {
                    // Add ripple effect
                    const ripple = document.createElement('span');
                    ripple.style.position = 'absolute';
                    ripple.style.borderRadius = '50%';
                    ripple.style.background = 'rgba(255, 255, 255, 0.3)';
                    ripple.style.transform = 'scale(0)';
                    ripple.style.animation = 'ripple 0.6s linear';
                    ripple.style.left = '50%';
                    ripple.style.top = '50%';
                    ripple.style.width = '100%';
                    ripple.style.height = '100%';
                    ripple.style.transform = 'translate(-50%, -50%) scale(0)';
                    
                    this.appendChild(ripple);
                    
                    setTimeout(() => {
                        ripple.remove();
                    }, 600);
                });
            });
        });

        // Add ripple animation
        const style = document.createElement('style');
        style.textContent = `
            @keyframes ripple {
                to {
                    transform: translate(-50%, -50%) scale(2);
                    opacity: 0;
                }
            }
        `;
        document.head.appendChild(style);
    </script>
</footer>